export type FoundUser = {
  username: string;
  email: string;
  password: string;
  createdAt: string;
  dob: string;
};

export type User = {
  id: string;
  position: number;
  username: string;
};
